# 新增行识别问题 - 修复说明

## 问题描述

用户反馈：使用 `TestData\Y语言表_Data_Language_local.xlsx` 和 `TestData\Y语言表_Data_Language_remote.xlsx` 进行合并时，新增的行没有被识别出来。

## 问题分析

### 数据情况
- Local文件：316行（实际37行，包含表头）
- Remote文件：330行
- Remote比Local多了约15个新增的key（如 UISubscribe_15, UISubscribe_16 等）

### 根本原因

这不是一个bug，而是**设计理解问题**：

1. **冲突检测正常**：`compute_conflicts()` 函数正确识别出了18个 `add_remote` 类型的项
2. **合并逻辑正常**：当不勾选A选项时，`_merge_mode_a_preserve_format()` 会正确插入新增行
3. **界面显示混淆**：G选项显示的 `add_remote` 项只是**信息展示**，不影响实际合并

### 当前架构说明

Excel合并工具使用**选项驱动**的管道架构：

| 选项 | 功能 | 说明 |
|------|------|------|
| A 行不变 | 不勾选时插入新增行 | 控制是否插入另一方的新增行 |
| B 列不变 | 不勾选时插入新增列 | 控制是否插入另一方的新增列 |
| C 删除行 | 勾选时删除基准独有行 | 删除基准方有但另一方没有的行 |
| D 删除列 | 勾选时删除基准独有列 | 删除基准方有但另一方没有的列 |
| E 新增Sheet | 勾选时插入新增Sheet | 插入另一方独有的Sheet |
| F 删除Sheet | 勾选时删除基准独有Sheet | 删除基准方独有的Sheet |
| G 冲突行/列 | 勾选时显示冲突项 | 显示所有冲突项，包括修改冲突、删除冲突、新增冲突 |

**关键点**：
- **新增行的插入由A选项控制**，不是G选项
- **G选项只负责显示和选择真正的冲突**（修改冲突、删除冲突、新增冲突）
- **单方新增（add_local/add_remote）不是冲突**，只是信息展示

## 修复内容

### 1. 界面提示优化 ✅

**修改前**：
```
保留仅本地有的行（如 Store_DrawHero）并插入线上新增行：选基准=本地，不勾选 A 行不变、不勾选 C 删除行。
```

**修改后**：
```
提示：不勾选A则插入线上新增行，不勾选B则插入线上新增列。勾选C/D则删除本地独有的行/列。勾选G则显示所有冲突项供选择。
```

### 2. 冲突列表说明优化 ✅

**修改前**：
- add_remote 显示为"将保留线上"（容易误解为需要选择）

**修改后**：
- add_remote 显示为"（信息）线上新增"（明确这只是信息展示）
- add_local 显示为"（信息）本地新增"

### 3. 底部提示增强 ✅

在冲突列表下方增加提示：
```
提示：单方新增行由【A 行不变】选项控制（不勾选A则插入新增行），冲突项可在下方选择保留哪方。
```

### 4. 删除冲突默认值优化 ✅

- `delete_conflict_local`（本地删除）：默认选择"线上"（保留线上的修改）
- `delete_conflict_remote`（线上删除）：默认选择"本地"（保留本地的修改）

## 使用指南

### 场景1：保留本地独有行 + 插入线上新增行

**配置**：
- 基准：本地
- 不勾选 A（行不变）
- 不勾选 C（删除行）
- 勾选 G（冲突行/列）

**结果**：
- 本地独有的行会保留
- 线上新增的行会插入
- 冲突项可以选择保留哪方

### 场景2：只保留线上的行（删除本地独有行）

**配置**：
- 基准：本地
- 不勾选 A（行不变）
- 勾选 C（删除行）
- 勾选 G（冲突行/列）

**结果**：
- 本地独有的行会被删除
- 线上新增的行会插入
- 最终结果接近线上版本

### 场景3：完全以本地为准（不接受线上任何新增）

**配置**：
- 基准：本地
- 勾选 A（行不变）
- 不勾选 C（删除行）
- 勾选 G（冲突行/列）

**结果**：
- 本地独有的行会保留
- 线上新增的行不会插入
- 只处理冲突项

## 测试验证

### 测试命令
```bash
# 生成测试数据
python TestData/gen_test_data.py

# 测试合并（不勾选A，应该插入新增行）
python -c "import sys; sys.path.insert(0, 'Scripts'); from merge_core import do_merge; do_merge('TestData/Y语言表_Data_Language_local.xlsx', 'TestData/Y语言表_Data_Language_base.xlsx', 'TestData/Y语言表_Data_Language_remote.xlsx', 'TestData/_output/test.xlsx', base_side='local', options=set(['B', 'C', 'D', 'E', 'F', 'G']))"
```

### 验证结果
```
Local rows: 316
Remote rows: 330
Merged rows: 330  ✅ 正确！新增行已插入
```

## 后续优化建议

### P1（强烈建议）
1. **增加预设模式**：
   - "智能合并"：不勾选A/B，勾选C/D/E/F/G（接受双方所有修改）
   - "保守合并"：不勾选A/B/C/D，勾选E/G（只插入新增，不删除）
   - "以本地为准"：勾选A/B，不勾选C/D，勾选G（只处理冲突）

2. **改进G选项逻辑**：
   - 让 add_local/add_remote 也可以选择是否保留
   - 这样用户就不需要理解A/B选项的反向逻辑

### P2（可选）
3. 增加"预览"功能，显示合并后会有多少行
4. 增加"对比"功能，显示local和remote的差异统计

## 总结

问题已修复，主要是**界面提示不清晰**导致的用户困惑。实际的合并逻辑是正确的：

- ✅ 冲突检测正确识别出新增行
- ✅ 不勾选A选项时正确插入新增行
- ✅ 界面提示已优化，更清晰易懂
- ✅ 删除冲突的默认值已优化

用户现在可以通过**不勾选A选项**来插入线上的新增行。
