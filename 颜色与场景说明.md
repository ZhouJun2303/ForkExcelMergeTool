# Excel 合并/对比 颜色与场景说明

> 配置 Fork、合并/对比操作步骤见 [README.md](README.md)。

**Git 约定**：LOCAL=ours=当前分支，REMOTE=theirs=被合并分支。冲突时取 LOCAL。

## 一、合并模式（Merged 输出）

合并结果为 MERGED 文件，对**新增**、**修改**、**冲突**行分别做颜色标注。

| 颜色 | 说明 | 场景 |
|------|------|------|
| 绿色背景 (#CCFFCC) | 新增：BASE 中不存在该 key | 仅 LOCAL 新增、仅 REMOTE 新增、两边同增 |
| 黄色背景 (#FFFF99) | 修改：BASE 存在，但内容有变更 | 仅 LOCAL 修改、仅 REMOTE 修改、两边同改 |
| 红色背景 (#FFCCCC) + 红色粗体 | 冲突：LOCAL 与 REMOTE 对同一 key 做了不同修改 | 两边异改、两边异增 |
| 无标注 | 无变化 | BASE = LOCAL = REMOTE |

**行顺序**：BASE 原有 key 保持 BASE 顺序，新增 key（仅 LOCAL 或仅 REMOTE 有）放到最后。

### 合并场景与处理规则

| 场景 | 说明 | 取谁 | 颜色标记 |
|------|------|------|----------|
| 无变化 | BASE = LOCAL = REMOTE | 任意 | 无 |
| 仅 LOCAL 修改 | BASE = REMOTE，LOCAL 不同 | LOCAL | 黄色(修改) |
| 仅 REMOTE 修改 | BASE = LOCAL，REMOTE 不同 | REMOTE | 黄色(修改) |
| 两边同改 | LOCAL = REMOTE，均与 BASE 不同 | 任意 | 黄色(修改) |
| 两边异改 | LOCAL ≠ REMOTE，均与 BASE 不同 | LOCAL | 红色(冲突) |
| LOCAL 删除 | BASE+REMOTE 有，LOCAL 无 | REMOTE | 修改/无（视 REMOTE 与 BASE 是否相同） |
| REMOTE 删除 | BASE+LOCAL 有，REMOTE 无 | LOCAL | 修改/无（视 LOCAL 与 BASE 是否相同） |
| 仅 LOCAL 新增 | BASE+REMOTE 无，LOCAL 有 | LOCAL | 绿色(新增) |
| 仅 REMOTE 新增 | BASE+LOCAL 无，REMOTE 有 | REMOTE | 绿色(新增) |
| 两边同增 | BASE 无，LOCAL = REMOTE | 任意 | 绿色(新增) |
| 两边异增 | BASE 无，LOCAL ≠ REMOTE | LOCAL | 红色(冲突) |

---

## 二、对比模式（Compare 输出）

对比结果为 `*_compare.xlsx`，按 Status 列分色。

| 颜色 | 色值 | Status | 说明 |
|------|------|--------|------|
| 绿色背景 | #CCFFCC | B新增 | 行仅在 B（右侧/REMOTE）中存在 |
| 红色背景 | #FFCCCC | A独有 | 行仅在 A（左侧/LOCAL）中存在 |
| 黄色背景 | #FFFF99 | 修改 | 行在 A、B 都有，但内容不同 |
| 无背景 | — | 相同 | 行在 A、B 都有且内容相同 |

### 对比模式列说明

- `[Key]`：第一列 key
- `[A-LEFT]`：A 文件（LOCAL）该行内容
- `[B-RIGHT]`：B 文件（REMOTE）该行内容
- `[Status]`：A新增 / B新增 / 修改 / 相同

---

## 三、测试数据对应（gen_test_data.py）

| Key | 合并场景 | 合并结果 | 对比 Status |
|-----|----------|----------|-------------|
| k1 | 无变化 | 保持 | 相同 |
| k2 | 仅 LOCAL 修改 | 取 LOCAL | 修改 |
| k3 | 仅 REMOTE 修改 | 取 REMOTE | 修改 |
| k4 | 两边同改 | 取同值 | 相同 |
| k5 | 两边异改 | 取 LOCAL 标红 | 修改 |
| k6 | LOCAL 删除 | 取 REMOTE | B新增 |
| k7 | REMOTE 删除 | 取 LOCAL | A独有 |
| k8 | 仅 LOCAL 新增 | 取 LOCAL | A独有 |
| k9 | 仅 REMOTE 新增 | 取 REMOTE | B新增 |
| k10 | 两边同增同值 | 取同值 | 相同 |
| k11 | 两边异增 | 取 LOCAL 标红 | 修改 |
</think>
修正表格中 k6 的说明：
<｜tool▁calls▁begin｜><｜tool▁call▁begin｜>
StrReplace