# Excel合并工具 - Bug修复与优化建议

## 一、核心问题分析

### 1. 冲突显示逻辑问题

**当前问题**：
- k6场景：BASE有，LOCAL删除，REMOTE保留 → 显示为"将保留线上"（绿色新增）
- k8场景：BASE无，LOCAL新增，REMOTE无 → 显示为"将新增行"（绿色新增）
- 两种完全不同的语义（删除冲突 vs 单方新增）却用相同的颜色和描述

**根本原因**：
在 `conflict.py` 的 `compute_conflicts()` 中，"仅本地有"和"仅线上有"的行被简单标记为 `_only_local` 或 `_only_remote`，没有区分：
- 是"新增"（BASE无，某方有）
- 还是"删除冲突"（BASE有，某方删除）

### 2. 界面设计问题

**当前问题**：
- 7个选项（A-G）+ 基准选择，用户需要理解复杂的组合逻辑
- 颜色图例使用橙色表示"修改"，但实际代码中用黄色
- 冲突列表中"Key / 说明"列混合显示key和操作描述，不够清晰
- 缺少对"删除-新增冲突"的特殊视觉提示

## 二、优化方案

### 方案1：修复冲突检测逻辑（推荐）

**目标**：明确区分4种场景
1. 单方新增（BASE无，某方有）→ 绿色，"将新增"
2. 单方删除（BASE有，某方删除）→ 灰色，"将删除"
3. 删除-保留冲突（BASE有，一方删除，一方保留/修改）→ 橙色，"需选择：删除 vs 保留"
4. 双方修改冲突（BASE有，双方都改且不同）→ 红色，"需选择：本地 vs 线上"

**实现要点**：
```python
# 在 conflict.py 中增强冲突分类
for key in all_keys:
    row_l = dict_l.get(key)
    row_r = dict_r.get(key)
    row_b = dict_b.get(key)
    
    # 场景1：BASE无，双方都有 → 新增冲突
    if row_b is None:
        if row_l and row_r:
            if not row_equal(row_l, row_r):
                conflicts.append({
                    "sheet": sheet_name,
                    "key": key,
                    "type": "add_conflict",  # 新增冲突
                    "local_row": row_l,
                    "remote_row": row_r,
                })
        elif row_l:
            conflicts.append({
                "sheet": sheet_name,
                "key": key,
                "type": "add_local",  # 仅本地新增
                "local_row": row_l,
            })
        elif row_r:
            conflicts.append({
                "sheet": sheet_name,
                "key": key,
                "type": "add_remote",  # 仅线上新增
                "remote_row": row_r,
            })
    
    # 场景2：BASE有，一方删除
    elif row_l is None or row_r is None:
        if row_l is None and row_r is None:
            # 双方都删除，无冲突
            pass
        elif row_l is None:
            # 本地删除，线上保留/修改
            conflicts.append({
                "sheet": sheet_name,
                "key": key,
                "type": "delete_conflict_local",  # 本地删除冲突
                "base_row": row_b,
                "remote_row": row_r,
            })
        else:
            # 线上删除，本地保留/修改
            conflicts.append({
                "sheet": sheet_name,
                "key": key,
                "type": "delete_conflict_remote",  # 线上删除冲突
                "local_row": row_l,
                "base_row": row_b,
            })
    
    # 场景3：BASE有，双方都有但不同
    else:
        if not row_equal(row_l, row_r):
            if not row_equal(row_b, row_l) and not row_equal(row_b, row_r):
                conflicts.append({
                    "sheet": sheet_name,
                    "key": key,
                    "type": "modify_conflict",  # 修改冲突
                    "local_row": row_l,
                    "remote_row": row_r,
                    "base_row": row_b,
                })
```

**界面显示优化**：
```python
# 在 merge_gui.py 中根据 type 显示不同颜色和文字
type_config = {
    "add_local": ("将新增（仅本地）", "new", "#008000"),
    "add_remote": ("将新增（仅线上）", "new", "#008000"),
    "add_conflict": ("新增冲突：需选择", "conflict", "#CC0000"),
    "delete_conflict_local": ("删除冲突：本地删除 vs 线上保留", "del_conflict", "#FF6600"),
    "delete_conflict_remote": ("删除冲突：线上删除 vs 本地保留", "del_conflict", "#FF6600"),
    "modify_conflict": ("修改冲突：需选择", "conflict", "#CC0000"),
}
```

### 方案2：简化界面（推荐）

**目标**：降低用户认知负担

**改进点**：
1. **预设模式**：提供3个常用预设
   - "智能合并"（默认）：A+B+C+D+E+F+G全选
   - "仅解决冲突"：只选G
   - "保守合并"：只选A+B+E+G（不删除任何内容）

2. **颜色图例统一**：
   ```python
   legend_merge = make_color_legend(center, [
       ("#008000", "绿色=新增"),
       ("#FF6600", "橙色=删除冲突"),
       ("#CC0000", "红色=修改冲突"),
       ("#808080", "灰色=将删除"),
   ])
   ```

3. **冲突详情面板优化**：
   - 双击冲突项时，显示3列对比：BASE | LOCAL | REMOTE
   - 对于删除冲突，特别标注"BASE存在，某方删除"
   - 增加"保留双方"选项（对于新增冲突）

### 方案3：性能优化

**当前问题**：
- 每次切换选项都重新加载整个workbook
- 大文件时界面卡顿

**优化**：
```python
# 在 MergeWindow.__init__ 中缓存workbook
self.wb_local = openpyxl.load_workbook(path_local, data_only=False)
self.wb_base = openpyxl.load_workbook(path_base, data_only=False)
self.wb_remote = openpyxl.load_workbook(path_remote, data_only=False)

# 在 _fill_content_by_options 中复用缓存
# 不再每次都 load_workbook
```

## 三、实施优先级

### P0（必须修复）
1. ✅ 修复冲突类型识别逻辑（区分删除冲突和新增）
2. ✅ 统一颜色图例和实际标记颜色

### P1（强烈建议）
3. ✅ 增加预设模式，简化用户选择
4. ✅ 优化冲突详情面板，显示BASE列
5. ✅ 性能优化：缓存workbook

### P2（可选）
6. 增加"保留双方"选项（对于新增冲突）
7. 增加撤销功能
8. 增加合并预览（不生成文件，只显示结果）

## 四、测试用例补充

建议增加以下测试场景：
- k12: BASE有，LOCAL删除，REMOTE删除 → 无冲突，直接删除
- k13: BASE有，LOCAL删除，REMOTE修改 → 删除冲突
- k14: BASE有，LOCAL修改，REMOTE删除 → 删除冲突
- k15: BASE无，LOCAL新增A，REMOTE新增B → 新增冲突（内容不同）
